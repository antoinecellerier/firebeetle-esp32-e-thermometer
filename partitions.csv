# Single-app partition table, both boards (4MB flash, no OTA — flashed over USB).
# Referenced from BOTH build paths so they agree:
#   - platformio.ini board_build.partitions (PIO generates the table from this)
#   - sdkconfig.defaults CONFIG_PARTITION_TABLE_CUSTOM_FILENAME (idf.py)
#
# `history` holds the temperature archive (see src/HistoryStore.cpp). An upload
# rewrites only the bootloader, this table, and the app, so it survives
# reflashing; only `esptool erase_flash` wipes it.
#
# Its offset comes FIRST and must never change: growing it later pushes `factory`
# up and leaves already-written archive content where it is. Placing it after
# `factory` would mean any app-slot resize orphans years of data.
#
# Name,   Type, SubType,  Offset,   Size
nvs,      data, nvs,      0x9000,   0x6000
phy_init, data, phy,      0xf000,   0x1000
history,  data, 0x40,     0x10000,  0x1E0000
factory,  app,  factory,  0x1F0000, 0x200000
coredump, data, coredump, 0x3F0000, 0x10000
