.pio
.vscode/.browse.c_cpp.db*
.vscode/c_cpp_properties.json
.vscode/launch.json
.vscode/ipch
.vscode/settings.json
core
sdkconfig.*
!sdkconfig.defaults
!sdkconfig.defaults.*
managed_components/
include/generated/
*.swp

# Claude Code local-only state (keep .claude/ itself tracked so shared
# settings / agents / skills can be checked in when desired)
.claude/settings.local.json
.claude/projects/
.claude/worktrees/

# Bench scratch: partition images, backups, saved configs. Multi-MB binaries.
/scratchpad/
.venv

# Archive images pulled off a device by tools/history.py backup
hist-*.bin

# ppk2.py sweep output (raw captures + reports; cited from docs/notes.md by dirname)
/ppk2-sweep-*/

# Byte-compiled pre-build helpers (pre_build.py imports them under SCons)
scripts/__pycache__/
